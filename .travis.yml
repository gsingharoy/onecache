language: go
go:
 - 1.8.x

before_install:
  - go get -u github.com/kardianos/govendor && govendor install

script:
 - govendor test -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)
